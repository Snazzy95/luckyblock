for i,v in pairs(game:GetService("ReplicatedStorage"):GetChildren()) do
    if v:IsA("RemoteEvent") and string.match(tostring(v), "SpawnGalaxy") then
        for i2 = 0,10 do
            game:GetService("ReplicatedStorage")v:FireServer()
        end
    end
end